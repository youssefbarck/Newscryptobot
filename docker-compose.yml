version: "3.8"

services:
  news-bot:
    build: .
    container_name: whale-news-bot
    restart: unless-stopped
    env_file: .env
    ports:
      - "${PORT:-10000}:10000"
    volumes:
      - bot-data:/tmp

volumes:
  bot-data: